version: '3.9'

services:
#  catalog.api:
#    image: ${DOCKER_REGISTRY-}catalogapi
#    build:
#      context: .
#      dockerfile: Services/Catalog/Catalog.API/Dockerfile

  catalog-db:
    image: postgres

volumes:
  postgres_catalog:
