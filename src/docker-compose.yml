
services:
#Catalog
  catalog-db:
    image: postgres

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile

#Basket
  basket-db:
    image: postgres

volumes:
  postgres_catalog:
  postgres_basket:


